version: 0.1

endpoints:
  - name: OarChat Socket Endpoint
    port: 8080
    type: REST
    networkVisibility: Public
    # no context means it listens on root "/"
    # no schemaFilePath means no OpenAPI validation
